{
  "name": "basket-league-proj",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "prop-type": "^0.0.1",
    "query-string": "^6.1.0",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-router-dom": "^4.3.1",
    "react-scripts": "@2.0.0",
    "react-transition-group": "^2.4.0",
    "slug": "^0.9.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "firebase-init": "firebase login && firebase init",
    "deploy": "npm run build && firbase deploy"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "firebase-tools": "^6.2.2"
  }
}
